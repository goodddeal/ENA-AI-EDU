[project]
name = "broadcast-ott"
version = "0.1.0"
description = "방송 프로그램 편성 OTT 연결 모바일 웹 MVP"
readme = "broadcast_ott_service_planning.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.32.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.uv]
package = false
